[build]
  base = "frontend"  # Directorio base donde se encuentra tu código fuente
  command = "npm run build"  # Comando para construir el proyecto
  publish = "frontend/dist"  # Directorio de salida de tu build

[[redirects]]
  from = "/api/*"
  to = "http://localhost:8444"
  status = 200
  force = true
